/**
 * Toolbox > Position section's own "Alignment" control — a placeholder, not yet wired to any value.
 */

import { cloneTemplateFragment } from '../../controls/template';

export function renderAlignment() {
  return cloneTemplateFragment('editrix-control-alignment');
}
