/**
 * Toolbox > Advanced section's own "CSS Classes" control — a placeholder, not yet wired to any value.
 */

import { cloneTemplateFragment } from '../../controls/template';

export function renderCssClasses() {
  return cloneTemplateFragment('editrix-control-css-classes');
}
